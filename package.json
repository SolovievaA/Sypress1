{
  "name": "cypress-test",
  "version": "1.0.0",
  "description": "first tests with Cypress",
  "main": "index.js",
  "scripts": { 
    "note:headed": "npx cypress run --browser edge --headed cypress.config.js",
    "note:headless": "npx cypress run --browser chrome --headless cypress.config.js",
    "mobile:headed": "npx cypress run --browser chrome --headed --config-file cypress2.config.js",
    "mobile:headless": "npx cypress run --browser edge --headless --config-file cypress2.config.js"
  },
  "author": "Solovieva Alla",
  "license": "ISC",
  "dependencies": {
    "cypress": "^13.13.3",
    "@faker-js/faker": "^8.4.1"
  }
}
